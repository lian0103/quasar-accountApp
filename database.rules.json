{
  "rules": {
		"spend":{
      "$uid":{
        ".read": "auth.uid == $uid", 
    		".write": "auth.uid == $uid"
      }
    },
    "tags":{
      "$uid":{
        ".read": "auth.uid == $uid", 
    		".write": "auth.uid == $uid"
      }
    }
  }
}